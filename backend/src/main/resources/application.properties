spring.application.name=crkt11

# H2 in-memory DB for development
spring.datasource.url=jdbc:h2:mem:crkt11db
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

server.port=8080

# JWT (override in prod via env vars)
jwt.secret=${JWT_SECRET:crkt11-dev-secret-key-change-in-production}
jwt.expiration-ms=86400000
